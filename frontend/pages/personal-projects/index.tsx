import type { NextPage } from "next";
import { PersonalProjects } from "../../components/PersonalProjects/PersonalProjects";

const PersonalProjectsPage: NextPage = () => {
  return <PersonalProjects />;
};

export default PersonalProjectsPage;
